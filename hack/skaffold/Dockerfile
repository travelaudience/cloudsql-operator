FROM gcr.io/distroless/base

# Use the pre-built binary in "build/cloudsql-operator" (i.e. the one generated by running "make build").
# This is done so we can take advantage of local cache directories, and makes skaffold re-deploy the application only when it is recompiled.
COPY build/cloudsql-operator /cloudsql-operator

CMD ["/cloudsql-operator", "-h"]
